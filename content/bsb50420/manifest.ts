import type { ManifestSchema } from "@cpp/config";

export const manifest: ManifestSchema = {
    home: { headline: "BSB50420. Choose an RTO with receipts." },
    links: { methodology: "/methodology", appeal: "/appeal" },
    faq: { rewritten_faqs: [] }
};
